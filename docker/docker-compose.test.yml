version: '3'
services:
    sut:
        build:
            context: ../
            dockerfile: docker/Dockerfile
        volumes:
            - ../tests:/usr/local/airflow/tests
        entrypoint: pytest
