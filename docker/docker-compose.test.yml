version: '3.2'
services:
    sut:
        build:
            args:
                cache_tag: $CACHE_TAG
            context: ../
            dockerfile: docker/Dockerfile
            cache_from:
                - $CACHE_TAG
                - $IMAGE_NAME
        volumes:
            - ../tests:/usr/local/airflow/tests
        entrypoint: pytest
